# CLAUDE.md - RES-Slim

## Scope
Private fork of Reddit Enhancement Suite v5.24.8, stripped to comment tweaks and media expandos. Targets old.reddit.com. Chrome MV3 + Firefox MV2.

## Build
- `yarn install`
- `yarn once` — dev build to `dist/chrome/`
- `yarn build` — production build, zips to `dist/zip/`
- Load unpacked from `dist/chrome/` in Chrome.

## Kept modules
Comment: `hideChildComments`, `commentNavigator`, `commentPreview`, `commentTools`, `commentQuickCollapse`, `commentSortBy`, `commentStyle`, `commentDepth`, `commentHidePersistor`, `saveComments`, `hover`, `showParent`, `readComments`, `newCommentCount`, `spoilerTags`, `noParticipation`, `sourceSnudown`, `context`.
Media: `showImages` + all 87 `lib/modules/hosts/` handlers.
Appearance: `nightMode` (minimal — just the body class toggle + FOUC guard; upstream sunrise/sunset/subreddit-whitelist stripped).
Infra: `menu`, `notifications`, `settingsNavigation`, `selectedEntry`, `version`, `requestPermissions`, `search` (hidden via `module.hidden = true` so the sidebar entry is gone but the in-console search box still functions).

## Key files
- `lib/modules/index.js` — single aggregator. Add/remove a module by toggling its import + export here.
- `lib/modules/hosts/index.js` — media host aggregator. To trim a host: delete its import + export line and delete the file.
- `build.js` — esbuild driver. Strips flow types, sass-compiles, replaces manifest tokens.
- `package.json` — `title` / `name` / `version` flow into manifests at build time via `__name__` etc. tokens.
- `chrome/manifest.json`, `firefox/manifest.json` — token templates. Cloud OAuth host permissions removed.

## Notes / gotchas
- `hosts/youtube.js` contains a hardcoded Google API key for read-only video metadata. Not tracking, not changed.
- Modules deleted from `lib/modules/index.js` but their supporting folders in `lib/modules/<name>/` were also wiped where present (`filteReddit/`, `backupAndRestore/`).
- `core/migrate/migrate.js` retains migration step records — RES upstream branding strings in older entries were updated only where user-facing.
- Other locale files (`locales/locales/*.json`) left as-is. Missing keys fall back to `en.json`.
- Extension ID differs from upstream RES (new `package.json` name) — existing RES users will not inherit settings.
- No CI workflow. Build locally and load unpacked / side-load zip.

## Trimming host handlers later
Each host is one file in `lib/modules/hosts/` and one import+export line in `lib/modules/hosts/index.js`. To drop a host: remove both. No other wiring.

## Version
- v0.4.0 — Comprehensive QA audit. **Build system**: fixed `noSourcemap` parameter name mismatch (Firefox/Opera always got sourcemaps); fixed unhandled promises in multi-browser builds (`Promise.all`); fixed `undefined` token injection for missing `browserMobileMinVersion`; removed dead `chromebeta`/`edge`/`opera` targets whose manifests don't exist. **Manifests**: removed dead `webRequest` permission from Chrome MV3; removed redundant `script-src-elem` from Chrome CSP; removed unused `cookies`/`identity` permissions from Firefox MV2. **Modules**: added missing `module.include` to `fixImageLinks` and `downloadButtons` (ran on options page); fixed `archiveLinks` Unddit URL double-`/r/` bug; fixed `reddEye` `hideFlagged` using bare `.collapsed` class instead of clicking expand toggle (same bug previously fixed in `hideGifComments` v0.3.7); fixed `viewDeleted` detached-DOM check using `thing.entry` instead of `thing.element`; fixed `nextTopComment` duplicate button injection on re-navigation. **Settings console**: fixed `a.target += ' _parent'` concatenation bug (produced invalid target like `'_blank _parent'`); fixed `noreferer` typo → `noreferrer` across `settingsConsole.js`, `noParticipation.js`, and `showImages/templates.js`. **Settings console (data)**: fixed table keycode options dropping the `metaKey` bit (4-element array instead of 5). **CSS**: removed 26 dead SCSS imports for stripped modules (accountSwitcher, keyboardNav, filteReddit, dashboard, etc.) — **50KB** smaller res.css. **Dependencies**: removed dead `suncalc` and `favico.js` (upstream nightMode sunrise/sunset and notification badge features were stripped in v0.1.0). Module count: 45.
- v0.1.0 — initial fork
- v0.2.0 — gear icon opens settings console directly; removed About/Search-RES-Settings sidebar entries; removed troubleshooter; restored minimal nightMode (dark theme) as default landing
- v0.3.0 — 16 new modules ported from the old.reddit userscript ecosystem: commentHighlights, autoExpand, nextTopComment, hideGifComments, absoluteTimestamps, reddEye, viewDeleted, fixImageLinks, fixProcessingImg, downloadButtons, restoreSubCounts, archiveLinks, infiniteScroll, markAllRead, userProfileSearch, subredditBlacklist. All fresh rewrites — AGPL sources used as reference only. Module count: 43.
- v0.3.1 — settings console UX improvements: module enable/disable toggle with permission prompts, disabled-module scrim, category-panel navigation tweaks, search input tightening, console restyling.
- v0.3.2 — two new appearance modules (classicFavicon, disableSubredditStyles) + stability fixes across the v0.3.0 ecosystem ports + substantial options.scss restyling pass. Module count: 45.
- v0.3.3 — fresh extension icon set: minimalist RS monogram on dark charcoal circle with Reddit-orange accent bar. Toolbar action icons redesigned to match. New build/generate-icons.py (Pillow) regenerates the full set.
- v0.3.4 — settings workspace UX refinement (Codex): ~500 lines of options.scss updates, templates.js and settingsConsole.js polish, search.js simplification, stage.js staging additions.
- v0.3.5 — QA audit pass. Fixed: `disableSubredditStyles` typo (`ref=` → `rel=`) that left inline `<style title="applied_subreddit_stylesheet">` blocks in place; `absoluteTimestamps`/`userProfileSearch`/`restoreSubCounts` missing `module.include` so they ran on the extension's own options page; `infiniteScroll` now explicitly registers appended things with RES's watcher system so downstream modules see them; `fixProcessingImg` guards against walking a detached comment subtree after ajax and CSS.escape's the image-id selector; `fixImageLinks` observer is now module-scoped (was unreferenced); `markAllRead` fails loudly when no modhash is available instead of silent "All marked read"; `restoreSubCounts` replaces `innerHTML` with DOM APIs; settings console keycode modal now resets `captureKey` on blur (prevented an armed window-level keydown listener from eating keystrokes after console close); `saveCurrentModuleOptions` wraps `stage.commit()` in try/catch and surfaces failures via notifications instead of lying "All changes saved".
- v0.3.9 — Rewrote `tests/settingsConsole.js` nightwatch fixtures. The upstream RES test suite referenced modules that were stripped from RES-Slim in v0.1.0 (`accountSwitcher`, `keepLoggedIn`, `quickMessage`, `wheelBrowse`), so the tests couldn't actually run against this fork. Remapped each test to a module that actually exists: boolean/text/table tests now target `commentDepth` (the only RES-Slim module with a `table` option type — the `subredditCommentDepths` table with list/text/text sub-fields), enum test now targets `absoluteTimestamps.format` (locale/iso), module-disable test now targets `autoExpand`, discard-reset test now targets `commentHighlights.borderOnly`, and the nested-click test uses `commentHighlights` as the target. All selectors already match the v0.3.6 global-save layout (`#RESGlobalSave`, `#RESGlobalStageBar`, `#RESGlobalDiscard`, `.is-saved`, `.is-dirty`). Still no CI wiring — the suite is here for forward consistency if someone revives nightwatch later.
- v0.3.8 — `settingsNavigation` click handler and URL parser fixes. (1) Clicks with any modifier key (ctrl/cmd/shift/alt) now bypass the interceptor so the user's "open in new tab" etc. works as expected — previously only `ctrlKey` was respected. (2) The click handler uses `e.target.closest('a')` instead of `e.target instanceof HTMLAnchorElement`, so clicks on children of settings links (icons, nested spans) still navigate to the settings console. (3) Anchors with `target="_blank"` are left alone so explicit new-tab links work. (4) `parseHash` and `makeUrlHash` now use `encodeURIComponent`/`decodeURIComponent` instead of a brittle regex with `%20` literal replacement, so special characters in option keys round-trip correctly; also normalizes the legacy `#!settings` prefix internally. (5) Prefix match in `parseHash` now requires the prefix to be followed by `/` or end-of-string — previously a URL like `#res:settings-redirect-standalone-options-page/accountSwitcher` (a distinct test-harness route that happens to share the `#res:settings` prefix as a literal substring) would get parsed with `-redirect-standalone-options-page` as the moduleID.
- v0.3.7 — QA audit on v0.3.6 settings console + second-look on v0.3.5 modules. Fixed: `viewDeleted` now guards against writing into a detached comment subtree after ajax (same pattern as `fixProcessingImg`); `hideGifComments` was only adding the `.collapsed` class but never triggering Reddit's native collapse — now clicks the `.expand` toggle to run Reddit's own collapse logic; filter chip ARIA changed from the incomplete `role="tab"` / `aria-selected` pattern (would have needed `role="tabpanel"` + arrow-key nav + `aria-controls`) to `role="group"` + `aria-pressed` on each chip, which is the correct semantic for toggle-style filter buttons; `.moduleRowToggle` thumb on-state margin was `16px` but should be `18px` so the thumb mirrors the 2px off-state gap; at the `680px` responsive breakpoint, `.globalSaveButton` and `.globalDiscardButton` had `justify-content: center` applied (meaningless on non-flex buttons) — switched to `text-align: center`; added a sidebar empty state ("No modules match this filter." + "Show all modules" reset button) so an empty filter result doesn't look like the module list broke; `drawConfigOptions` now clears the stale `$moduleOptionsScrim` reference when skipping scrim creation for no-option modules so subsequent `.toggleClass` calls don't mutate a detached ghost; manually collapsing the currently-active category now drops the `.active` highlight so the visual state doesn't read as "this category is still selected" while hidden.
- v0.3.6 — Settings console UX transformation. **Categories no longer accordion-collapse** — every category stays expanded by default so the full 45-module list is always scannable. Click the category header to collapse just that one (chevron flips). **Every module row now has an inline toggle switch in the sidebar**, so you can enable/disable without the old two-click navigate-then-toggle dance; `alwaysEnabled` modules show a lock dot instead of a toggle. **Filter chip row above the module list** (`All / On / Off / Modified` with live counts) lets you see just the modules you've touched, or just the disabled ones, etc. **Global Save + Discard + stage-status bar in the top header**, replacing the per-module workspace Save button — unsaved changes are visible/actionable regardless of which module you're viewing; dirty state is warning-colored, saved state is success-colored, with a pulse animation on commit. Dropped the `/r/Enhancement` sidebar link (wrong for a private fork that explicitly stripped upstream RES), the "Settings" eyebrow, and the marketing subtitle. Dropped the now-redundant `#moduleOptionsSave`, `.workspaceStageStatus`, `.moduleActionRow`, `.moduleWorkspaceEyebrow`, `#RESConsoleSubredditLink` from options.scss. Added `.globalStageBar`, `.globalSaveButton`, `.globalDiscardButton`, `.filterChips`, `.filterChip`, `.moduleRow`, `.moduleRowToggle`, `.moduleRowLock`, `.categoryButtonChevron`, `.sidebarFooter`. New `syncSidebarModuleState(moduleID, enabled)` keeps the sidebar toggle visually in sync with stage + commit operations; `toggleModuleEnabled(id)` is the single shared code path for enabling/disabling (used by both sidebar and workspace toggles, plus by global save/discard when re-syncing). Discard now uses `Options.stage.reset()` and redraws the current workspace.
